# nfl-frontend (legacy)

The original React + Vite frontend, kept for reference only.

**This app is not deployed and is not the one to work in.** The active
frontend is [`primary-ui`](../primary-ui) — see the
[root README](../README.md).

It was a minimal viewer for prediction cards served directly from the FastAPI
backend, before the current UI replaced it.
